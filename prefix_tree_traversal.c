/* Traverse a prefix tree and print out all subnets which match
 * the input subnet and mask. 
 */
 
 void prefix_tree_traversal(node* root, int addr, int mask) {
    
    if (root == NULL) {
        return;
    }
    
}
        
